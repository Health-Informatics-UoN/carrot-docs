image: python:latest
pages:
  stage: deploy
  only:
    - master
  script:
    - pip install mkdocs-material
    - pip install mknotebooks
    - mkdocs build --site-dir public
  artifacts:
    paths:
      - public
